meta {
  name: Health
  type: http
  seq: 1
}

get {
  url: {{base_url}}/health
  body: none
  auth: none
}

tests {
  test("service is healthy", function() {
    expect(res.status).to.equal(200);
    expect(res.body.status).to.equal("ok");
  });
}
